This project is a design-to-code implementation built with Next.js and Bootstrap.
The main goal is to convert a given UI design into a clean, responsive, and maintainable frontend application.The project follows a modular and component-based structure to keep the codebase clean and scalable.

To run the project locally, follow these steps:

npm install
npm run dev
